# DESIGN.md — 절기 스케치북

> UI를 만들거나 고치기 전에 반드시 읽는다. 모든 색·타이포·문구는 여기서 파생한다.
> 참조 스타일: Pa'lais (styles.refero.design, "botanical sketchbook dipped in honey")
> × 절기 달력. 하늘의 레퍼런스 선택으로 확정된 방향이다.

## 컨셉

손그림 요리책의 낱장이 계절 따라 넘어가는 앱. Pa'lais에게서 **질감**(크림 종이,
보태니컬 라인아트, 유기적 형태, 단일 잉크색의 규율)을, 절기 달력에게서
**시간**(절기 아이브로, 철마다 물드는 액센트)을 가져온다.

**시그니처:** 쪽빛 라인아트 스케치가 놓인 머리말 — 절기가 적히고, 계절 액센트가
물든다.

과감한 **장식**은 여전히 머리말 하나뿐이다. 카드의 품목 도판은 장식이 아니라 식별
장치이고, 그래서 조용할 의무 대신 **읽힐 의무**를 진다 (2026-08-24 개정 — 그 전엔
"과감함은 여기에만 / 나머지는 조용히"였는데, 카드마다 자연색 96px 도판이 들어오면
그 문장은 그냥 거짓이 된다).

## 색

**규율 (Pa'lais에게 배운 것): 텍스트·링크·버튼은 오직 쪽빛이 소유한다.**
계절 웜 컬러는 절대 글자를 싣지 않는다 — 배지 배경, 블롭, 칩 배경으로만 흐른다.
등락 마커(`--ink`/`--rise`)는 의미 전달로서 글자에 실린다.

공통 (계절 무관):

| 토큰 | 값 | 용도 |
|---|---|---|
| `--paper` | `#FBF9F6` | 배경. 크림 종이 (레퍼런스로 확정된 선택) |
| `--card` | `#FFFFFF` | 카드 — 크림 위에 순백 레이어링 |
| `--ink` | `#2B4586` | 쪽빛(藍). 모든 텍스트·링크·강조. 유일한 채도 있는 쿨톤 |
| `--muted` | `#7B84A3` | 보조 텍스트 (쪽빛의 뮤트) |
| `--line` | `#E7E2D6` | 구분선 (따뜻한 모래빛) |
| `--axis` | `#CBC2B0` | 스파크라인 x축 |
| `--margin` | `#E3C4B8` | 장보기 노트 여백선 |
| `--rise` | `#C0392B` | 러스트 / 가격 상승 마커(칩·큰가격) |
| `--rise-lo` | `#EFEAE3` | 상승 칩 저채도 배경 |

계절 액센트 (`body[data-season=…]` — 먹을 수 있는 웜 컬러만):

| 계절 | 월 | `--accent` | `--tint` | 심상 |
|---|---|---|---|---|
| spring | 3–5 | `#A2D3A6` | `#EAF4E9` | 새순 세이지 |
| summer | 6–8 | `#FFC400` | `#FFF4CE` | 꿀·참외 |
| autumn | 9–11 | `#ED7328` | `#FBE7D6` | 감·번트 오렌지 |
| winter | 12–2 | `#BC6E79` | `#F6E7EA` | 팥죽 |

`--accent`는 절정 배지 배경·블롭·카드 열림 테두리에, `--tint`는 칩과
머리말 블롭에. 글자는 그 위에서도 항상 쪽빛이다.

**품목 도판의 자연색은 이 규율 밖이다** (2026-08-24). 복숭아는 붉고 오이는 초록이다.
규율의 본문("텍스트·링크·버튼은 오직 쪽빛")은 그대로 서 있다 — 이미지는 배경
도형(블롭·마스킹테이프·칩 배경)처럼 색을 가질 뿐이고, **이미지 위에 글자를 얹지
않는다**는 조건이 그걸 보장한다. 그라데이션 금지도 유지된다: 에셋이 하드 엣지
색면(그라데이션 0)이라 실질적으로 지켜진다.

## 질감 — 보태니컬 라인아트

- 쪽빛 단색 스트로크의 잎가지 스케치(SVG, `currentColor`)를 머리말에 놓는다.
  토일(toile) 자수처럼 — 채우기 없이 선만, 낮은 불투명도(0.3~0.4)
- 머리말 뒤에 계절 `--tint` 색의 유기적 블롭 하나 (CSS border-radius 블롭 —
  자산 불필요)
- 일러스트는 과용하지 않는다: 머리말 1점, (선택) 빈 화면 1점. 카드 안엔 장식
  일러스트를 두지 않는다. 단 정보 그래픽(볼드 가격·등락 칩·가격 스파크라인·
  과일/채소 필터)은 장식이 아니라 데이터 전달이므로 카드에 허용된다.
- **품목 도판도 같은 논리로 허용된다** (2026-08-24). 카드 표지의 96px 실사 벡터
  도판은 장식이 아니라 **식별 장치**다 — 이모지가 그 일을 못 해서 들인 것이지
  카드를 꾸미려고 들인 게 아니다(🥬 하나가 다섯 품목을, 🐟 하나가 여섯을 썼다).
  머리말 스케치 1점 규칙은 그대로다.
- 열린 방향: 계절별 스케치 4종(봄 매화가지, 여름 열매가지, 가을 이삭, 겨울 마른가지)

## 타이포그래피

| 역할 | 폰트 | 사용처 |
|---|---|---|
| 본문·헤딩 | **Wanted Sans** (self-host woff2, 콘텐츠 서브셋, OFL) | 아이브로·h1·품목명·소제목·본문·가격 등 거의 전부. 웨이트 400/600/700/800 |
| 손글씨 | **학교안심 받아쓰기 L** (self-host woff2, 서브셋, OFL) | 카드 표지 하단 "제철이의 한마디"(`.why`) 한 줄만 — 유일한 캐릭터 폰트 |
| 라벨 | 본문 스택 + `letter-spacing: 0.08em` | "고르는 법"·"레시피" 같은 소형 라벨 |
| 숫자 | 본문 스택 + `font-variant-numeric: tabular-nums` | 가격 |

폰트 파일은 `src/fonts/`, CSS 상대 경로 참조 (Vite가 base 처리). CDN 금지.
못 구하면 시스템 고딕(`Apple SD Gothic Neo` …)으로 폴백.

**콘텐츠 서브셋:** 본문·손글씨 모두 전체 한글이 무거워, 콘텐츠(`produce.json`·`season.ts`·
`recipes.json`·`nutrition.json`·정적 UI 문구)에 실제 쓰인 글자만 서브셋한다(본문 4벌 ~240KB,
손글씨 ~30KB). 가격이 CI
JSON에서 오듯 폰트도 콘텐츠를 진실의 원천으로 삼는다. 문구를 고쳐 새 음절이 들어오면
`npm run subset:fonts`로 재생성한다 — 안 하면 `tests/font-coverage.test.ts`가 실패한다
(조용한 폴백 대신 시끄러운 실패).

## 절기

머리말 아이브로: `소서 · 7월 둘째 주`. 절기 날짜는 표기용 고정표
(`src/season.ts`) — 이 앱에서 절기는 시계가 아니라 계절 인사말이다.

## 레이아웃

- **모바일(~48rem)**: 단일 컬럼, 최대폭 28rem. **데스크탑(48rem~)**: 최대폭 64rem,
  카드가 다열 격자(열 하한 14rem — 768에서 3열, 1024에서 4열)로 흐르는 좌판.
  크림 캔버스 위 순백 카드. 카드 모서리는 종이처럼 crisp하게 (`border-radius: 0.2rem`)
  — 둥근 타일이 아니라 메모 낱장.
- 카드 표지는 폭에 따라 **두 얼굴**을 갖는다. 모바일은 가로형(도판 96px 옆에 이름),
  데스크탑은 세로 스택(도판 120px이 카드 폭 전체를 쓰는 표지, 그 아래 이름·가격·띠·한마디).
  전환은 다열 격자와 **같은 한 지점(48rem)**에서 일어난다 — 각 표지는 자기에게 맞는
  폭에만 산다. 자세한 근거는 아래 결정 기록(2026-09-06).
- 구분은 여백과 1px 선. **그라데이션 금지.** 그림자는 "메모가 냉장고에 붙어 들뜬
  표현에 한해 아주 옅은 1단계(`--lift`, 카드에만)"만 예외. 그 외 그림자는 금지.
- 카드는 **좌우로 넘기는 4쪽 무한 페이저**다(표지 / 시세·영양 / 고르는 법 / 보관·쓰임).
  아래로 펼치지 않는다 — 카드 높이가 변하지 않는 게 이 설계의 전부다. 넘김은 카드 테두리까지
  꽉 차게(full bleed) 미끄러진다. 무JS·프리렌더는 지금까지의 `<details>` 그대로이고,
  하이드레이션 후에야 페이저로 승격한다 (카테고리 세그먼트가 "하이드레이션 후 JS radiogroup"인
  것과 같은 패턴). 자세한 근거는 아래 결정 기록(2026-09-06).

> 구현 메모: 마크업은 `src/components/`의 React(JSX) 컴포넌트다. 토큰·팔레트·리셋은
> `src/global.css`(Tailwind `@theme static`), 시그니처 효과(의사요소·`@keyframes`·스케일 밖 기하)는
> `src/components/*.module.css`, 평범한 레이아웃은 JSX Tailwind 유틸리티 — 세 곳의 하이브리드
> 경계는 `CLAUDE.md` "스타일 경계" 참고. 팔레트·타이포·규율은 프레임워크와 무관.

## 모션·접근성

- 모션은 절제해서 — 상태 전환을 부드럽게 하거나 시그니처를 살리는 데만. `prefers-reduced-motion: reduce`면 전부 끈다:
  - **카드 쪽 넘김**: 스프링(ζ≈0.87, response≈0.35s, `requestAnimationFrame`). 제스처가
    끌면 1:1로 따라오고, 놓으면 그 속도를 이어받아 안착한다. 날던 중에 잡으면 그 자리에서
    이어진다. 한 제스처는 한 쪽만 넘어간다. reduced-motion이면 즉시.
    (`<details>` 폴백에는 기존 `::details-content` 리빌이 그대로 남아 있다)
  - **필터칩·레시피칩**: 켜짐/꺼짐 색 전환 150ms
  - **레시피 메모**: 열기/닫기 대칭 1쌍(오버슈트 없음)
  - **차양(NavIndex)**: 그리드 높이 언롤 300ms(`cubic-bezier(0.2,0.75,0.2,1)`) + 램프줄 당김
  - **카테고리 세그먼트(ButtonGroup)**: 선택 알약이 칸 사이를 슬라이드(차양과 같은 곡선 `cubic-bezier(0.2,0.75,0.2,1)` 320ms) + 라벨 색 전환. reduced-motion이면 즉시.
  - **머리말 스케치**: 첫 로드 획 드로우인 0.7s(가지→잎→열매, 매 로드) — 시그니처 예외
- `:focus-visible`에 쪽빛 2px 아웃라인
- 웜 컬러 위 글자는 항상 쪽빛 — 명도 대비 4.5:1 이상을 색 규율이 보장한다

## 문구

CLAUDE.md의 규칙(한국어, 담백, 이커머스 화법 금지)에 더해:

- 계절의 목소리로: "7~8월이 절정이에요" ○ / "지금이 구매 적기!" ✕
- 감탄사·느낌표 금지. 서술로 끝낸다
- 실패·빈 화면도 같은 목소리로: "이번 달 제철 정보가 아직 없어요"

## 결정 기록

- 초안은 "절기 달력"(일러스트 없는 고요한 미니멀)이었다. 하늘이 Pa'lais
  레퍼런스를 가져와 논의 후 융합으로 확정 (2026-07-10)
- 크림 종이는 AI 기본값 팔레트와 겹치지만, 레퍼런스로 지정된 선택이므로 따른다.
  대신 쪽빛 단일 잉크 규율 + 계절 시스템 + 한글 부리체가 이 디자인의 고유성을 만든다
- 계절 가변 팔레트는 유지 — Pa'lais는 고정 브랜드지만, 이 앱의 주제는 계절이다
- 카드의 한마디(`whyNow`)를 펼침 영역 최상단에서 **표지 하단**으로 내리고 손글씨체로
  바꿨다 (2026-07-11). "제철이가 종이에 연필로 적어준 한 줄"이라는 심상 — 펼치기 전에도
  늘 보인다. 서명("— 제철이")은 넣지 않는다: 매 카드 반복되면 시끄럽고, 제철이는
  문구의 목소리로만 존재한다. 색은 규율대로 쪽빛의 뮤트(`--muted`), 웜 컬러 아님
- 카드를 냉장고 장보기 메모처럼 개편했다 (2026-07-11). 상단 마스킹테이프(반투명 계절색)+
  옅은 부양(`--lift`)+±0.4° 미세 기울기+crisp 모서리. 앱의 "장보기 메모" 은유를 눈에
  보이게. 그림자 금지 규율을 메모 고정 한정으로 완화(그라데이션 금지는 유지).
  스펙: `docs/superpowers/specs/2026-07-11-fridge-memo-card-design.md`
- 레시피 표시를 바텀시트에서 **카드 위 핀 메모 한 장**으로 바꿨다 (2026-07-12). 진입점은
  레시피별 칩(횡스크롤, `.chip-btn`), 상세는 압정으로 꽂힌 낱장(`.memo`) + `‹ ›` 넘김(clamp).
  냉장고-메모 은유의 연장. 그림자(메모 부양 1단)와 모션(열기/닫기 대칭 1쌍, 오버슈트 없음)에
  예외를 하나씩 더 열었다 — 마스킹테이프 완화와 같은 결. 압정은 단색(그라데이션 금지 유지).
  apple-design 스킬 렌즈로 다듬음(열기≠넘기기 분리, ~44px 터치 타깃, 칩 누름-즉시 피드백).
  스펙: `docs/superpowers/specs/2026-07-12-recipe-pin-memo-design.md`
- "다가오는 제철"을 메인 하단 한 줄에서 **별도 경로**(`/coming`)로 세우고, 두 페이지를
  **램프줄(A) + 차양 드롭 인덱스**(`NavIndex`)로 넘나들게 했다 (2026-07-12). 초안은 좌/우
  옆면 세로 인덱스 탭이었으나, 브라우저 실측에서 (1) 좌/우 배치가 페이지 넘김 연상을
  못 주고 (2) 모바일 단일 컬럼에서 고정 탭이 본문(품목 이모지)을 가려 폐기. 중간안이던
  풀하이트 슬라이드 드로워도 "화면을 덮는다"는 이유로 HTML 시안 비교 후 폐기하고, 우상단
  램프줄을 당기면 목차가 그 줄에서 아래로 풀려 내리는 차양(shade) 컴팩트로 확정 — 본문을
  가리지 않는다. 램프줄은 라인아트 어휘(선+끝점 원, 머리말 `Sprig`와 라임)를 그대로 쓰고
  iOS 아일랜드·유광 질감·햄버거 3줄(앱-크롬 클리셰)은 피했다. 줄을 뷰포트 위로 오버슈트시켜
  당겨도 위쪽에 틈이 생기지 않는다.
- 다가오는 페이지는 처음엔 냉장고-메모 카드의 표지 껍데기만 재활용한 얇은 `ComingCard`(가격·
  스파크라인·영양·레시피·펼침 없는 정적 `<div>`)였으나, 이후 메인과 **동일한 `ProduceCard`**로
  통합했다(`ComingCard` 폐기). 간트·펼침·손질법·영양·레시피는 메인과 같고, 가격만 작년 같은
  시기 씨앗(`coming-prices.json`)이라 등락 칩·스파크라인 없이 "작년 기준" 한 줄로만 보인다 —
  예고는 "볼 것"이지 "살 것"이 아니므로 등락 신호가 없는 게 자연스럽다. 마스킹테이프 색은 그
  **품목의 미래 달 계절색**(8월 품목은 여름 노랑, 9월 품목은 가을 오렌지)으로 물들여 "달력을
  미리 넘겨보는" 느낌을 색으로 얹었다. 이를 위해 계절 토큰 규칙을 `body[data-season]`(전역 한
  계절)에서 `[data-season]`(임의 요소)로 일반화 — `body`는 여전히 전역 기본을 주고, 카드마다
  그 위에 자기 계절을 재정의한다.
- 가격에 **기준선(단위) 줄**을 넣었다 (2026-07-14). `100g 기준` / `10개 기준 · 개당 704원`.
  단위가 없으면 감자 `315원`(100g)과 수박 `21,476원`(1개)이 같은 저울 위에 있는 것처럼
  읽힌다 — 단위는 장식이 아니라 숫자를 해석 가능하게 만드는 최소 조건이다.
  자리는 **큰 숫자 아래 muted 한 줄**(기존 개당값 자리를 확장). 개당값과 같은 계층인 게
  개념적으로 정확하다 — 둘 다 "이 숫자를 어떻게 읽나"를 말하는 각주다.
  시안 3개를 만들어 비교 후 확정. 탈락안과 이유:
  - **접두형**(`100g에 315원`, 한국어 어순대로 읽힘)은 자리를 만들려면 등락 칩을 취소선
    줄로 올려야 한다 → **머리기사인 "내려왔다"가 부제로 강등된다.** 대가가 너무 크다.
  - **라벨형**(취소선 줄 왼쪽에 작은 라벨)은 `100g  370원`이 한 줄에 붙어 라벨이 **취소선
    가격에 걸린 것처럼** 보인다. 명확히 하려다 새 모호함을 들인다. 라벨체(넓은 자간 소형)도
    냉장고 메모가 아니라 스프레드시트의 목소리다.
  엣지: 한 달 전 가격이 없으면(복숭아) 취소선·칩이 통째로 사라지는데 **기준선 줄은 남는다** —
  단위는 조건부가 아니다. 가격 자체가 없으면(KAMIS 미조사) 잴 것도 없으니 줄도 없다.
- 목차 링크는 평범한 `<a>` 대신 **TanStack `<Link viewTransition>`**(클라이언트 라우팅)로
  바꿨다. 초안의 `<a>`는 전체 페이지 하드 리로드라 "/"↔"/coming" 전환이 뚝 끊겼다(브라우저
  실측으로 확인). `Link`는 리로드 없이 `document.startViewTransition`으로 부드럽게 넘기고
  basepath도 자동 처리한다. 직접 URL 진입·무JS 폴백을 위해 CSS 크로스도큐먼트 view
  transition은 유지. `prefers-reduced-motion: reduce`면 차양 언롤·램프줄 당김·전환 전부 즉시로.
  스펙: `docs/superpowers/specs/2026-07-12-coming-seasonal-index-tab-design.md`
- 카드에 **제철 띠(간트 바)**를 넣었다 (2026-07-18). 이모지·이름 바로 아래(왼쪽 `id-wrap`
  열, 반폭 좌측정렬, `<summary>` 안이라 접힘/펼침 모두 표시). 12개월 축 위에 제철=연한
  계절색(`--tint`)·절정=짙은 계절색(`--accent`)으로 얹는 **짙음/연함 간트** — 같은 계절색의
  두 명도. 축선(`--line`)은 바 세로 중앙을 지나고, 시작·끝점을 1·12월 숫자 중앙에 맞춰 양끝
  반 칸(`100%/24`)씩 들였다. 월 숫자는 **제철 월과 이번 달만**(모노, 이번 달만 잉크 볼드),
  나머지는 숨겨 조용히. 색 규율 준수(웜=배경, 잉크는 숫자에만, 그라데이션 없음). 파생은
  `src/card.ts` `toSeasonStrip`(순수), 표시는 `SeasonStrip`. 브라우저 시안 반복(색채움→모노
  해치→도트→색채움→간트)으로 확정. 스펙: `docs/superpowers/specs/2026-07-18-season-strip-on-card-design.md`
- 타이포를 **Wanted Sans 하나로 통일**하고 마루부리(세리프)를 완전히 제거했다 (2026-07-21).
  세리프는 h1·아이브로 헤더에만 갇혀 카드에선 증발했고, 카드 콘텐츠에 얹으면 소제목이 튀었다.
  시안 3라운드(세리프 세 얼굴 → 손글씨만 빼고 고딕 → 캐릭터 고딕 세 후보 앱 전반) 끝에,
  Pretendard급 완성도에 온기를 더한 Wanted Sans로 확정. 손글씨는 유일한 캐릭터 폰트로 승격.
  콘텐츠 서브셋(마루부리 865KB보다 648KB 가벼움) + 커버리지 빌드 가드. 이때 손글씨 서브셋
  121자 드리프트(수산물·축산물 whyNow 미재생성)도 발견·수정. 스펙:
  `docs/superpowers/specs/2026-07-21-body-font-wanted-sans-design.md`
- 카드 펼침에 **실제 리빌 모션**을 붙였다 (2026-07-22). 그전까진 `<details>`가 내용을 즉시
  스냅해 열고 테두리 색만 전환됐다 — DESIGN.md가 예정한 "카드 펼침 전환"이 실은 비어 있었다.
  `::details-content` + `interpolate-size: allow-keywords`로 무JS 높이 아코디언(Chrome 131+,
  미지원은 즉시 펼침 = 뷰 트랜지션과 같은 선택적 향상). 브라우저 프레임 실측으로 세 함정을 잡았다:
  (1) `.open`의 `margin-top`이 애니메이션 박스 밖으로 새어(마진 병합) 닫힘 끝에 16px "덜거덕" →
  구분선을 `border-top`에서 `::before`로 옮기고 위·아래 간격을 padding·flex gap으로만(마진 금지).
  (2) 앞부분에 몰린 차양 곡선(`0.2,0.75,0.2,1`)이라 열림이 100ms에 92% 끝나 "무전환"으로 보임 →
  큰 리빌은 표준 이징(`0.4,0,0.2,1`) 240ms, 작은 차양은 스냅 곡선 유지(크기별 구분).
  (3) 절대배치 레시피 메모는 containing block이 `details`라 `::details-content`의 `overflow:clip`에
  안 잘림(실측 확인). 픽스처·순수로직 무관, CSS만 변경.
- 필터칩 상태 전환·머리말 스케치 드로우인을 추가했다 (2026-07-22). 필터칩은 켜짐/꺼짐 색을
  레시피칩과 동일하게 150ms 전환(칩 일관성, scale 없음—텍스트 들썩임 방지). 머리말 스케치
  (시그니처, "과감함은 여기에만")는 첫 로드 때 가지→잎→열매 순으로 획이 그려진다(0.7s,
  `pathLength="1"` 정규화 스태거) — 매 로드 재생하되 0.35 획이라 조용하다. reduced-motion이면 셋 다 정적.
- 과일·채소·수산물 카테고리를 FilterBar 알약 칩에서 **세그먼트 ButtonGroup**으로 분리했다
  (2026-07-23). 카테고리는 상호배타(항상 하나)인데 상태 칩과 시각이 같아 "여럿 켤 수 있다"로
  오독됐다. `[전체 | 과일 | 채소 | 수산물]` 단일 선택 세그먼트(radiogroup, ←/→ 방향키, 슬라이딩
  알약 썸)로 바꿔 "하나 고르는 곳"을 시각 규율로 만들고, 상태 칩(chip group)과 대비시켰다.
  '전체' 칸을 추가해 미필터를 명시적 선택으로 표현. 트랙에 **inset 음영**을 얹었다 — 냉장고
  메모 부양(`--lift`)에 이은 **두 번째 그림자 예외**(안으로만, 세그먼트 웰의 눌린 홈; 그라데이션
  금지는 유지). apple-design 렌즈로 시안 3안(트랙+슬라이딩 / 밑줄 탭 / 아웃라인) 중 트랙안,
  이어 음영 3안(톤 / inset / 결합) 중 inset을 브라우저 시안 비교로 확정. 스펙:
  `docs/superpowers/specs/2026-07-23-category-button-group-design.md`
  구현 후 브라우저 실측으로 다듬음: 풀폭 대신 **내용폭 좌측 정렬**(등폭 고정 칸, `align-self:start`)로
  상단 밀도를 낮추고, 크기를 한 단계 줄였다(라벨 `--text-xs`, 상태 칩도 `--text-sm`→`--text-xs`로 맞춤).
  트랙이 낮아지며 생긴 반경 어긋남은 **동심 규칙(안쪽 반경 = 바깥 −여백 3px)**으로 정합.
- **"순수 CSS 변수" 결정을 개정 — Tailwind `@theme static` 토큰 + 하이브리드로 전환했다**
  (2026-08-11). 2026-07-19 CSS Modules 코로케이션 스펙이 명시적으로 반려했던 Tailwind를,
  같은 스코프 문제 때문이 아니라 **다른 이유**로 다시 들였다: **작성 속도가 주된 근거**다 —
  클래스 이름을 짓고 파일을 오가지 않고 JSX에서 바로 스타일링한다. 에이전트 친화(AI 보조
  UI 작업의 산출물 품질)와 생태계 접근성(향후 shadcn/ui 등)은 보조 가설·미래 대비일 뿐,
  이번 결정을 떠받치는 건 아니다. 전면 전환은 아니다 — 앱 정체성의 상당 부분인 맞춤 효과
  (마스킹테이프 `color-mix`, 홀짝 기울기 `nth-child`, `::details-content` 리빌, 계절 간트
  `calc`, `@keyframes`)는 여전히 유틸리티와 싸우므로 **시그니처는 CSS 모듈에 남긴다**(경계
  기준 셋은 `CLAUDE.md` "스타일 경계" 참고). Tailwind의 기본 리셋(Preflight)은 **가져오지
  않는다** — `<ol>` 마커·헤딩 굵기 등 이 앱이 기대는 UA 기본값을 지워 "픽셀 동일" 원칙을
  즉시 깨기 때문이다. 토큰 값은 한 글자도 재설계하지 않았다(간격·타입 스케일의 리듬 보존
  유지). 스펙: `docs/superpowers/specs/2026-08-10-tailwind-migration-design.md`
  (`docs/superpowers/specs/2026-07-19-css-modules-colocation-design.md`의 부분 개정).
- 카드의 품목 표식을 이모지에서 **실사 기반 벡터 도판 96px**으로 바꿨다 (2026-08-24).
  이모지는 톤이 아니라 **기능**이 문제였다: 🥬 하나가 애호박·배추·양배추·상추·얼갈이배추
  다섯을, 🐟 하나가 고등어 외 다섯을 썼고 김은 🍙(주먹밥)이었다. 카드를 훑을 때 표식이
  품목을 못 가리키고 같은 그림이 반복돼 목록이 뭉갰다. 게다가 이모지는 플랫폼 폰트라
  앱의 나머지 전부(토큰·폰트·라인아트)가 통제되는데 가장 먼저 눈에 들어오는 요소만
  남의 것이었다.
  컨셉은 시안 3안을 브라우저에서 비교해 **시장 좌판 스틸라이프**(색면 20–40단계,
  좌상단 방향광, 자연색 무보정)로 확정. 탈락: **인쇄 도판**(색면 8–12)은 실사의 풍성함을
  스스로 깎고, **계절 표본 카드**(색면 5–8)는 96px에서 무료 아이콘팩 톤이 났다. 확정 후
  인쇄 도판을 한 장 실제로 뽑아 A/B까지 해봤는데, 96px에서 형태는 더 잘 지키지만 붉은
  볼이 '물든 자국'이 아니라 '오려붙인 도형'으로 읽혀 식욕을 잃었다 — 그리고 그 유일한
  장점("축소를 견딘다")은 1x 기준이라 Retina에서 절반쯤 사라진다.
  **접지 그림자는 뺐다.** 70장 중 생선 측면도·낱잎·부위 단면·건어물은 바닥에 놓여 있지
  않아 그림자가 붕 뜨고(실측 확인), 그림자가 켜지는 순간 그림이 *종이에 인쇄된 것*에서
  *종이 위에 놓인 물건*으로 바뀌어 냉장고 메모가 스크랩북에서 스티커북 쪽으로 이동한다.
  방향광만으로 입체가 서므로 그림자 없이도 납작해지지 않는다.
  표지 레이아웃도 함께 개편했다(안 B-2): 도판+이름 / 가격 전폭 가로 행 / 제철 띠 전폭 /
  한마디. 마크·이름·가격을 한 행에 두는 건 산술적으로 불가능했다 — 카드 안쪽 324.4px에서
  가격 열 하한 120.3px을 빼면 이름에 82.5px밖에 안 남는다. 이 개편에 딸려 기존 버그 둘도
  고쳤다: 제철 띠 축척이 카드마다 1.72배 흔들리던 것(`width:60%`가 `.idWrap` 종속),
  그로 인해 갈치·물오징어의 두 자리 월 라벨이 겹쳐 `7 8 91011`로 읽히던 것.
  스펙: `docs/superpowers/specs/2026-08-22-produce-image-assets-design.md`
- **데스크탑 다열 격자와 세로 스택 카드 표지**를 넣었다 (2026-09-06). 그전까진 폭이 어디서든
  28rem 단일 컬럼이라, 데스크탑에선 화면 대부분이 크림 여백이었다. 최대폭을 64rem으로 열고
  카드를 `auto-fill minmax(14rem, 1fr)` 격자로 흘린다(768에서 3열, 1024에서 4열 — 두 경우
  카드 폭이 228px로 같다. 폭은 그대로 두고 열 수만 는다).
  **카드 variant를 만든 이유는 폭이 넓어져서가 아니라 좁아져서다.** 처음엔 "3열이면 열 폭이
  지금 카드 폭과 비슷하니 카드는 그대로 두면 된다"고 봤는데, 실측하니 1024/3열의 카드 안쪽은
  281px로 **지금(324px)보다 43px 좁았다**. 표지 안 B-2의 산술(가격 열 하한 120.3px)을 그대로
  빼면 이름에 39.5px밖에 안 남는다 — "한우 등심"이 깨지는 폭이다. 게다가 가로형은 좁은 열에서
  첫 행([도판 96 | 이름])의 오른쪽이 텅 빈다. 그래서 그 첫 행마저 세로로 눕혀, 도판이 카드 폭
  전체(96 → 120px)를 쓰는 표지로 바꿨다. 도판이 커진 게 이 variant의 진짜 이득이다 —
  도판은 장식이 아니라 식별 장치인데(2026-08-24), 좌판처럼 늘어놓으니 그 역할을 훨씬 잘 한다.
  나머지 행(가격·제철 띠·한마디)의 순서·구조는 손대지 않았다. 눕는 건 첫 행뿐이다.
  **브레이크포인트는 하나(48rem)**다. 가로형은 좁은 열에서 첫 행이 비고, 세로 스택은 단일
  컬럼에서 한 화면에 1.5장밖에 안 들어온다 — 둘은 같은 지점에서 교대해야 각자 맞는 폭에만 산다.
  크롬도 함께 넓어진다(시안 B): 컨트롤이 한 줄로 눕는다(검색·세그먼트·필터칩·정렬). 다만
  **머리말만 34rem으로 폭을 잡는다** — 시그니처(블롭+스케치)는 제목 오른쪽 어깨에 얹힌 한
  덩어리인데, 머리말이 64rem을 다 쓰면 블롭이 우측 끝까지 밀려 제목과 700px 떨어진 채 혼자 뜬다
  (브라우저 실측). 가운데 정렬이 아니라 좌측에 그대로 앉은 채 폭만 잡는 것이라 시안 A와 다르다.
  시안 3안(A 좌판 = 크롬 28rem 중앙 / B 전폭 / C 좌측 sticky 크롬 + 2열)을 카드 2종과 조합해
  브라우저에서 비교 후 B·세로스택·1024 캡으로 확정:
  `docs/prototypes/2026-09-06-desktop-multicolumn.html`
  **남은 것**: 격자에서 카드를 펼치면 그 행 전체의 높이가 펼친 카드만큼 커져, 같은 행의 나머지
  칸이 최대 700px 빈다(실측). 격자 아코디언의 통상 동작이고 "낱장 한 장을 펼쳐 읽는다"는 은유엔
  맞지만, 구멍이 큰 건 사실이다. masonry는 미출시, CSS `columns`는 정렬 순서가 세로로 흘러
  "하락 큰 순"이 깨져 읽힌다 — 지금은 그대로 두고 별도 사이클로 미룬다.
- 카드를 **좌우로 넘기는 4쪽 무한 페이저**로 바꿨다 (2026-09-06). 아래로 펼치던 `<details>`를
  없앴다 — 데스크탑 다열 격자에서 카드를 펼치면 **그 행의 나머지 칸이 최대 700px 비었다**
  (`align-items:start`라 카드는 안 늘어나지만 행 높이가 펼친 카드를 따라간다). masonry는 미출시고
  CSS `columns`는 정렬 순서가 세로로 흘러 "하락 큰 순"이 깨져 읽힌다. 페이저는 이 구멍을
  **0으로** 만든다 — 카드 높이가 애초에 변하지 않기 때문이다(실측: 어느 쪽에서도 400px).
  모바일도 같이 바꿨다. 단일 컬럼엔 구멍 문제가 없지만 지금도 펼치면 700px이 자라 스크롤 위치를
  잃었고, 무엇보다 같은 카드가 폭에 따라 열림 모델이 다르면 혼란이다.
  **쪽 분할은 미감이 아니라 산술로 골랐다.** 고정 높이는 가장 긴 쪽이 정하므로, 표지가 최장으로
  남아야 기본 화면에 죽은 공간이 안 생긴다. `produce.json` 84개·`recipes.json` 75개 실측으로
  네 후보를 비교해(속쪽 여백 평균 6쪽 158px / 5쪽 125px / **4쪽 70px** / 손질통째 127px+표지여백
  50px) 4쪽으로 확정했다. 손질을 통째로 두면 **84개 중 39개(46%)**가 표지를 넘겨 모든 카드의
  표지 아래가 상시로 비므로 `고르는 법`과 `보관·쓰임`으로 갈랐다 — 고를 때(가게)와 집에 온 뒤는
  때가 다르니 의미로도 맞다. **레시피 조리 단계는 쪽에 안 들어간다**(75개 중 73개가 표지 초과,
  최장 872px, 순서라 쪼갤 수도 없다) — 지금의 카드 위 핀 메모 오버레이가 우연이 아니라 길어서
  생긴 것이었다. 쪽엔 진입 칩만 둔다. 이 부등식은 `tests/card-pages.test.ts`가 지킨다(문구가
  길어지면 조용히 여백이 생기는 대신 시끄럽게 실패한다 — 폰트 커버리지와 같은 결).
  속쪽마다 `[작은 도판 | 품목명 | 쪽 라벨]` 머리를 둔다: 4열 격자에서 여러 장을 각기 다른 쪽에
  넘겨두면 "시세"만 있고 무슨 품목인지 알 수 없다. 표지가 최장이라 남는 여백의 일부를 신원에 쓰는
  셈이다(접근성 트리엔 중복이라 `aria-hidden`).
  **Full bleed**: 가로 여백을 카드에서 쪽으로 옮겨(뷰포트는 음수 마진으로 카드 패딩 밖으로,
  `RecipeChips`가 이미 쓰는 관용구) 들어오는 쪽이 카드 가장자리에서 나온다. 안 그러면 16px 안쪽
  창의 "액자 속 슬라이드"가 된다.
  **무한 루프는 클론 없이** 한다 — 트랙은 가상 인덱스로 무한히 흐르고 각 쪽이
  `translateX(k·N·W)`로 건너뛰어 현재 위치 근처에 다시 선다. React에서 같은 컴포넌트를 두 벌
  렌더하지 않는다. **제스처도 라이브러리 없이** 60줄이다(`usePager.ts`): 1:1 추적 +
  `setPointerCapture`, 10px 히스테리시스(세로면 페이지 스크롤에 양보), Apple 모멘텀 투영,
  속도 인계, 중단 시 현재 값에서 재개. 한 제스처는 한 쪽만 — 세게 던지면 투영이 여러 쪽을
  건너뛰어 4쪽 루프에서 방향을 잃는다(iOS 페이징 스크롤과 같은 처리). 경계가 없으니 러버밴딩도 없다.
  **고정 높이에 JS 측정은 없다** — 트랙이 flex라 가장 긴 쪽이 높이를 잡고 네 쪽이 늘 흐름 안에
  있다. 대신 안 보이는 쪽은 `inert`로 빼서(스크린리더가 넷을 한꺼번에 읽거나 숨은 칩에 탭이
  걸리지 않게) `hidden`이 높이를 무너뜨리는 문제를 피한다.
  스펙: `docs/superpowers/specs/2026-09-06-card-paging-design.md`,
  시안: `docs/prototypes/2026-09-06-card-paging.html`
